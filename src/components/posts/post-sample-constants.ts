/** Shared styles for the fixed “sample” label — list, detail, route cards. */
export const POST_SAMPLE_BADGE_CLASS =
  "shrink-0 rounded-full border border-border/45 bg-background/58 px-2 py-0.5 text-[10px] font-semibold tracking-wide text-muted-foreground/90 backdrop-blur-sm";

/** Card / hero overlay on photos + dark gradient — more transparent so the image reads through. */
export const POST_SAMPLE_BADGE_ON_IMAGE_CLASS =
  "shrink-0 rounded-full border border-white/20 bg-black/22 px-2 py-0.5 text-[10px] font-semibold tracking-wide text-white/62 backdrop-blur-[2px]";
